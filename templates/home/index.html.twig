{# ============================================================================
   DASHBOARD / INICIO - Vista principal del panel de administración
   ============================================================================
   
   Ruta: /home
   Controlador: HomeController::index()
   Requiere: Autenticación (IS_AUTHENTICATED)
   
   Muestra:
   - Sección hero con bienvenida
   - Estadísticas: Total de categorías y productos
   - Cards con enlaces rápidos a Categorías y Productos
   - Información del stack tecnológico (Symfony, Bootstrap, Doctrine)
   - Últimos productos creados (tabla responsive)
   
   Características:
   - Diseño con gradientes y animaciones
   - Cards con hover effects
   - Stats cards con números grandes
   - Responsive design (Bootstrap Grid)
   - Iconos de Bootstrap Icons
#}
{% extends 'base_admin.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<style>
    .hero-section {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 4rem 0;
        margin-bottom: 3rem;
        border-radius: 0 0 50px 50px;
        box-shadow: 0 10px 40px rgba(102, 126, 234, 0.3);
        margin-top: 0;
    }
    
    .stats-card {
        border: none;
        border-radius: 20px;
        transition: all 0.3s ease;
        overflow: hidden;
        position: relative;
    }
    
    .stats-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 5px;
        height: 100%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    
    .stats-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    }
    
    .stats-number {
        font-size: 3rem;
        font-weight: 800;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    
    .module-card {
        border: none;
        border-radius: 20px;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        height: 100%;
        overflow: hidden;
        position: relative;
    }
    
    .module-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    .module-card:hover::before {
        opacity: 1;
    }
    
    .module-card:hover {
        transform: translateY(-15px) scale(1.02);
        box-shadow: 0 25px 50px rgba(0,0,0,0.2);
    }
    
    .module-icon {
        font-size: 5rem;
        margin-bottom: 1.5rem;
        transition: all 0.4s ease;
        position: relative;
        z-index: 1;
    }
    
    .module-card:hover .module-icon {
        transform: scale(1.2) rotate(10deg);
    }
    
    .btn-gradient-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        color: white;
        transition: all 0.3s ease;
    }
    
    .btn-gradient-primary:hover {
        background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        color: white;
    }
    
    .btn-gradient-success {
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        border: none;
        color: white;
        transition: all 0.3s ease;
    }
    
    .btn-gradient-success:hover {
        background: linear-gradient(135deg, #38ef7d 0%, #11998e 100%);
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(56, 239, 125, 0.4);
        color: white;
    }
    
    .feature-card {
        border: none;
        border-radius: 15px;
        transition: all 0.3s ease;
    }
    
    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px rgba(0,0,0,0.1);
    }
    
    .tech-icon {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        transition: all 0.3s ease;
    }
    
    .feature-card:hover .tech-icon {
        transform: scale(1.1);
    }
    
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
    }
    
    .floating {
        animation: float 3s ease-in-out infinite;
    }
    
    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }
    
    .pulse {
        animation: pulse 2s ease-in-out infinite;
    }
</style>

<!-- Hero Section -->
<div class="hero-section">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <h1 class="display-3 fw-bold mb-3" style="text-shadow: 2px 2px 4px rgba(0,0,0,0.2);">
                    ¡Bienvenido de nuevo! 👋
                </h1>
                <p class="lead mb-4" style="font-size: 1.3rem;">
                    Panel de administración potenciado por <strong>Symfony 7.1</strong> y <strong>Bootstrap 5</strong>
                </p>
                <div class="d-flex align-items-center">
                    <i class="bi bi-person-circle me-3" style="font-size: 2.5rem;"></i>
                    <div>
                        <p class="mb-1"><strong>{{ app.user.email }}</strong></p>
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            <span class="badge" style="background: rgba(255,255,255,0.3); font-size: 0.9rem;">
                                <i class="bi bi-star-fill text-warning"></i> Administrador
                            </span>
                        {% else %}
                            <span class="badge" style="background: rgba(255,255,255,0.3); font-size: 0.9rem;">
                                <i class="bi bi-person-fill"></i> Usuario
                            </span>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="col-lg-4 text-center d-none d-lg-block">
                <i class="bi bi-code-slash floating" style="font-size: 12rem; opacity: 0.2;"></i>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <!-- Estadísticas Rápidas -->
    <div class="row g-4 mb-5">
        <div class="col-md-6">
            <div class="card stats-card shadow-sm">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <p class="text-muted mb-2 text-uppercase" style="font-size: 0.85rem; font-weight: 600; letter-spacing: 1px;">
                                Total Categorías
                            </p>
                            <h2 class="stats-number mb-0">{{ totalCategorias }}</h2>
                            <p class="text-muted small mb-0 mt-2">
                                <i class="bi bi-arrow-up text-success"></i> Gestión de categorías
                            </p>
                        </div>
                        <div style="font-size: 4rem; opacity: 0.15;">
                            <i class="bi bi-folder-fill"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card stats-card shadow-sm">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <p class="text-muted mb-2 text-uppercase" style="font-size: 0.85rem; font-weight: 600; letter-spacing: 1px;">
                                Total Productos
                            </p>
                            <h2 class="stats-number mb-0">{{ totalProductos }}</h2>
                            <p class="text-muted small mb-0 mt-2">
                                <i class="bi bi-arrow-up text-success"></i> Inventario actual
                            </p>
                        </div>
                        <div style="font-size: 4rem; opacity: 0.15;">
                            <i class="bi bi-box-seam-fill"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Módulos del Sistema -->
    <div class="mb-5">
        <h2 class="fw-bold mb-4">
            <i class="bi bi-grid-3x3-gap-fill me-2" style="color: #667eea;"></i>
            Módulos del Sistema
        </h2>
        <div class="row g-4">
            <!-- Categorías -->
            <div class="col-md-6">
                <div class="card module-card shadow">
                    <div class="card-body text-center p-5">
                        <div class="module-icon">
                            <i class="bi bi-folder-fill" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"></i>
                        </div>
                        <h3 class="fw-bold mb-3">Categorías</h3>
                        <p class="text-muted mb-4">
                            Organiza y gestiona las categorías de tus productos. 
                            Crea estructuras jerárquicas para una mejor organización.
                        </p>
                        <div class="d-grid gap-2">
                            <a href="{{ path('app_categoria_index') }}" class="btn btn-gradient-primary btn-lg shadow">
                                <i class="bi bi-list-ul me-2"></i> Ver Todas las Categorías
                            </a>
                            <a href="{{ path('app_categoria_new') }}" class="btn btn-outline-primary btn-lg">
                                <i class="bi bi-plus-circle me-2"></i> Crear Nueva Categoría
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Productos -->
            <div class="col-md-6">
                <div class="card module-card shadow">
                    <div class="card-body text-center p-5">
                        <div class="module-icon">
                            <i class="bi bi-box-seam-fill" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"></i>
                        </div>
                        <h3 class="fw-bold mb-3">Productos</h3>
                        <p class="text-muted mb-4">
                            Administra tu catálogo completo de productos. 
                            Agrega, edita y elimina productos del inventario.
                        </p>
                        <div class="d-grid gap-2">
                            <a href="{{ path('app_producto_index') }}" class="btn btn-gradient-success btn-lg shadow">
                                <i class="bi bi-list-ul me-2"></i> Ver Todos los Productos
                            </a>
                            <a href="{{ path('app_producto_new') }}" class="btn btn-outline-success btn-lg">
                                <i class="bi bi-plus-circle me-2"></i> Crear Nuevo Producto
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Tecnologías -->
    <div class="mb-5">
        <h2 class="fw-bold mb-4 text-center">
            ⚡ Desarrollado con las mejores tecnologías
        </h2>
        <div class="row g-4">
            <div class="col-md-4">
                <div class="card feature-card shadow-sm h-100">
                    <div class="card-body text-center p-4">
                        <i class="bi bi-code-square tech-icon" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"></i>
                        <h5 class="fw-bold">Symfony 7.1</h5>
                        <p class="text-muted small mb-0">Framework PHP moderno, robusto y de alto rendimiento</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card feature-card shadow-sm h-100">
                    <div class="card-body text-center p-4">
                        <i class="bi bi-palette tech-icon" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"></i>
                        <h5 class="fw-bold">Bootstrap 5</h5>
                        <p class="text-muted small mb-0">Framework CSS responsive y componentes modernos</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card feature-card shadow-sm h-100">
                    <div class="card-body text-center p-4">
                        <i class="bi bi-database tech-icon text-warning"></i>
                        <h5 class="fw-bold">Doctrine ORM</h5>
                        <p class="text-muted small mb-0">Gestión avanzada de base de datos con ORM potente</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="card shadow-sm mb-5" style="border-radius: 20px; border: none; background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);">
        <div class="card-body p-4">
            <h5 class="fw-bold mb-4">
                <i class="bi bi-lightning-charge-fill text-warning me-2"></i>
                Acciones Rápidas
            </h5>
            <div class="row g-3">
                <div class="col-md-3">
                    <a href="{{ path('app_categoria_new') }}" class="btn btn-outline-primary w-100">
                        <i class="bi bi-folder-plus"></i><br>
                        <small>Nueva Categoría</small>
                    </a>
                </div>
                <div class="col-md-3">
                    <a href="{{ path('app_producto_new') }}" class="btn btn-outline-success w-100">
                        <i class="bi bi-box-seam"></i><br>
                        <small>Nuevo Producto</small>
                    </a>
                </div>
                <div class="col-md-3">
                    <a href="{{ path('app_categoria_index') }}" class="btn btn-outline-info w-100">
                        <i class="bi bi-list-ul"></i><br>
                        <small>Listar Categorías</small>
                    </a>
                </div>
                <div class="col-md-4">
                    <a href="{{ path('app_producto_index') }}" class="btn btn-outline-warning w-100">
                        <i class="bi bi-grid"></i><br>
                        <small>Listar Productos</small>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
