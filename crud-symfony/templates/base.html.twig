{# ============================================================================
   PLANTILLA BASE PARA PÁGINAS PÚBLICAS (Login y Registro)
   ============================================================================
   
   Esta plantilla se usa para páginas que NO requieren autenticación:
   - Login (/login)
   - Registro (/register)
   
   Características:
   - Fondo con gradiente púrpura/azul
   - Bootstrap 5.3.8 para estilos
   - Sin navbar ni footer
   - Diseño minimalista y moderno
   
   Las páginas autenticadas usan base_admin.html.twig
#}
<!DOCTYPE html>
<html>
    <head>
        {# Codificación de caracteres UTF-8 para soportar tildes y caracteres especiales #}
        <meta charset="UTF-8">
        
        {# Título de la página - Se puede sobrescribir en las plantillas hijas #}
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        {# Favicon (icono en la pestaña del navegador) - Logo de Symfony #}
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
       
        {# ========================================================================
           BOOTSTRAP 5.3.8 - Framework CSS
           ========================================================================
           Cargado desde CDN oficial de jsDelivr
           Proporciona: Grid system, componentes, utilidades CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">

        {# ========================================================================
           BLOQUE DE ESTILOS PERSONALIZADOS
           ========================================================================
           Las plantillas hijas pueden agregar estilos adicionales aquí #}
        {% block stylesheets %}
        <style>
            /* Fondo con gradiente moderno de púrpura a azul */
            body {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            }
        </style>
        {% endblock %}

        {# ========================================================================
           BLOQUE DE JAVASCRIPT
           ========================================================================
           Carga Asset Mapper y scripts personalizados #}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {# ========================================================================
           BLOQUE DE CONTENIDO PRINCIPAL
           ========================================================================
           Aquí las plantillas hijas (login.html.twig, register.html.twig)
           inyectan su contenido específico #}
        {% block body %}{% endblock %}

        {# ========================================================================
           BOOTSTRAP JAVASCRIPT
           ========================================================================
           Bundle que incluye Popper.js para tooltips, dropdowns, etc.
           Se carga al final para no bloquear el renderizado #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>

    </body>
</html>
