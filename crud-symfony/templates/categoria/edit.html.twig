{# ============================================================================
   EDITAR CATEGORÍA - Formulario de edición
   ============================================================================
   
   Ruta: /categoria/{id}/edit
   Controlador: CategoriaController::edit()
   Método HTTP: GET (mostrar formulario), POST (procesar actualización)
   Requiere: Autenticación
   
   Funcionalidades:
   - Formulario precargado con datos existentes de la categoría
   - Validación automática (NotBlank, Length)
   - Redirección a /categoria después de actualizar
   - Mensaje flash de éxito
   - Botón eliminar (con confirmación modal)
   - Botón volver al listado
   
   Variables recibidas:
   - {{ categorium }}: Entidad Categoria a editar
   
   Incluye:
   - categoria/_form.html.twig (formulario reutilizable)
   - categoria/_delete_form.html.twig (botón de eliminación)
#}
{% extends 'base_admin.html.twig' %}

{% block title %}Editar Categoría - {{ categorium.nombre }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-5">
                <i class="bi bi-pencil-square"></i> Editar Categoría
            </h1>
            <p class="text-muted">Modificar los datos de: <strong>{{ categorium.nombre }}</strong></p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0"><i class="bi bi-pencil"></i> Formulario de Edición</h5>
                </div>
                <div class="card-body">
                    {{ include('categoria/_form.html.twig', {
                        'button_label': 'Actualizar',
                        'button_class': 'btn btn-success'
                        
                        }) 
                    }}
                </div>
            </div>

            <div class="mt-3 d-flex gap-2">
                <a href="{{ path('app_categoria_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Volver al listado
                </a>
                
                <form method="post" 
                      action="{{ path('app_categoria_delete', {'id': categorium.id}) }}" 
                      style="display:inline;"
                      onsubmit="return confirm('¿Estás seguro de eliminar esta categoría?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorium.id) }}">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Eliminar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}